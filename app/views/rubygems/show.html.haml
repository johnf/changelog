
%p.name= @latest.name 
%p.version= @latest.version
%p.description= @latest.description

.versions
  Versions

  %ul
    - @rubygems.each do |rubygem|
      %li{:id => "version-#{rubygem.version.gsub(/\./, '_')}"}
        = rubygem.version
        - if rubygem.has_changelog?
          = link_to 'Changelog', changelog_rubygem_path(rubygem)
        -else
          No Changelog
